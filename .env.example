# Supabase (Readly project hrlpidapontbnrvmrqzw).
# Use the anon key only. Never put a service_role key in the frontend.
VITE_SUPABASE_URL=https://hrlpidapontbnrvmrqzw.supabase.co
VITE_SUPABASE_ANON_KEY=

# 1 = push/pull when a Supabase parent session exists.
# 0 or unset = stay on localStorage (the demo default).
VITE_READLY_CLOUD=0

# Optional separate LLM proxy. Coach LIVE uses the `coach` edge function
# when URL + anon key are set, and falls back to DEMO if the call fails.
# VITE_READLY_AI_URL=
